<MudPaper Class="@($"subscription-card-payment {(IsSelected ? "selected" : "")}")" OnClick="HandleSelect">

    <!-- Checkbox Top Right -->
    <div class="subscription-checkbox">
        <MudIcon Icon="@(IsSelected ? Icons.Material.Filled.CheckBox : Icons.Material.Filled.CheckBoxOutlineBlank)"
                 Class="@($"subscription-checkbox-icon {(IsSelected ? "selected" : "")}")" />
    </div>

    <!-- Plan Title -->
    <MudText Class="subscription-title">@Plan.PlanName</MudText>

    <!-- Price and Duration -->
    <MudText Class="subscription-price">
        @($"{Plan.Price:N0} QAR")
        <MudText Class="subscription-duration">/ @Plan.DurationName</MudText>
    </MudText>

    <!-- Select Button -->
    <MudBox Class="subscription-button-payment-container">
        <MudButton Class="@($"subscription-button-payment {(IsSelected ? "selected" : "")}")"
                   Disabled="@IsSelected"
                   OnClick="HandleSelect">
            @(IsSelected ? "Unselect" : "Select")
        </MudButton>
    </MudBox>

    <!-- Conditional Description -->
    <MudStack Class="subscription-description" Spacing="1">
        @if (Plan.PlanName?.Contains("Feature Ad") == true)
        {
            <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                Increased in visibility in the main page
            </MudText>
            <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                9x increase in views
            </MudText>
            <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                Best value feature
            </MudText>
        }
        else
        {
              <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                Increased in visibility in the main page
            </MudText>
            <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                9x increase in views
            </MudText>
            <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                Listed higher in search
            </MudText>
            <MudText Class="subscription-feature">
                <MudIcon Icon="@Icons.Material.Filled.Check" Class="subscription-check-icon" />
                Best value feature
            </MudText>
        }
    </MudStack>
</MudPaper>

@code {
    [Parameter] public PayToPublishPlan Plan { get; set; }
    [Parameter] public bool IsSelected { get; set; }
    [Parameter] public EventCallback<PayToPublishPlan> OnSelect { get; set; }

    private async Task HandleSelect()
    {
        await OnSelect.InvokeAsync(Plan);
    }
}

<style>
    .subscription-card-payment {
        border-radius: 8px;
        background-color: white;
        border: none ;
        height: 300px;
        padding: 20px;
        position: relative;
        transition: border-color 0.2s ease, box-shadow 0.2s ease;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

        .subscription-card-payment:hover {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }

        .subscription-card-payment.selected {
            border-color: #F26D1E;
        }

    .subscription-checkbox {
        position: absolute;
        top: 12px;
        right: 12px;
    }

    .subscription-checkbox-icon {
        font-size: 24px;
        color: #E0E0E0;
    }

        .subscription-checkbox-icon.selected {
            color: #F26D1E;
        }

    .subscription-title {
        font-family: 'Public Sans', sans-serif;
        font-size: 16px;
        font-weight: 600;
        color: #242424;
        text-align: center;
    }

    .subscription-price {
        font-family: 'Public Sans', sans-serif;
        font-size: 26px;
        font-weight: 600;
        color: #00426D;
        text-align: center;
    }

    .subscription-duration {
        display: inline-block;
        font-size: 14px;
        font-weight: 500;
        color: #646464;
        font-family: 'Public Sans', sans-serif;
    }

    .subscription-button-payment-container {
        display: flex;
        justify-content: center;
        margin-top: 12px;
        height: 40px;
    }

    .subscription-button-payment {
        width: 100%;
        max-width: 350px;
        font-weight: 600;
        background-color: #F26D1E;
        color: white;
        text-transform: none;
    }

        .subscription-button-payment.selected {
            background-color: #E0E0E0;
        }

    .subscription-description {
        margin-top: 12px;
    }

    .subscription-feature {
        font-size: 13px;
        color: #242424;
         font-family: 'Public Sans', sans-serif;
        display: flex;
        align-items: center;
    }

    .subscription-check-icon {
        margin-right: 4px;
        font-size: 18px;
        color: #2E7D32;
    }

</style>